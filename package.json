{
  "name": "nginx-gateway-microservices",
  "version": "1.0.0",
  "private": true,
  "description": "Containerized Nginx gateway microservices lab.",
  "scripts": {
    "compose:config": "docker compose config",
    "compose:test": "make test"
  }
}
