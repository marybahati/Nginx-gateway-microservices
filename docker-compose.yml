services:
  service-b:
    image: nginx-gateway-microservices-service-b:local
    build:
      context: .
      dockerfile: services/service-b/Dockerfile
    user: "node"
    environment:
      BIND_HOST: 0.0.0.0
      SERVICE_C_URL: http://service-c:3003
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3002/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - private

  service-c:
    image: nginx-gateway-microservices-service-c:local
    build:
      context: .
      dockerfile: services/service-c/Dockerfile
    user: "node"
    environment:
      BIND_HOST: 0.0.0.0
      SERVICE_A_CALLBACK_URL: http://service-a:3001
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3003/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - private

  service-a:
    image: nginx-gateway-microservices-service-a:local
    build:
      context: .
      dockerfile: services/service-a/Dockerfile
    user: "node"
    environment:
      BIND_HOST: 0.0.0.0
      SERVICE_B_URL: http://service-b:3002
      SERVICE_B_HEALTH_URL: http://service-b:3002/health
      SERVICE_C_HEALTH_URL: http://service-c:3003/health
    depends_on:
      service-b:
        condition: service_healthy
      service-c:
        condition: service_healthy
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3001/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - private

  nginx:
    image: nginxinc/nginx-unprivileged:1.27.5-alpine
    user: "101:101"
    ports:
      - "8080:8080"
    volumes:
      - ./nginx/nginx-docker.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      service-a:
        condition: service_healthy
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -q -O /dev/null http://127.0.0.1:8080/service-a/health || exit 1",
        ]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - public
      - private

networks:
  public:
    driver: bridge
  private:
    driver: bridge
    internal: true
